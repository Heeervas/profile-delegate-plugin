name: profile-delegate
version: "1.5.0"
description: "Profile Delegate 🤝: bounded cross-profile delegation with strict same-session transient recovery, deterministic child approvals, capability presets, origin-scoped inspection, and async support."
author: Adan
requires_env: []
provides_tools:
  - profile_delegate
  - profile_delegate_status
  - profile_delegate_list
  - profile_delegate_prune
provides_hooks: []
